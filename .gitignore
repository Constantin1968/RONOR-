node_modules/
dist/
.env
.env.local
*.env.local
coverage/
.DS_Store
*.log
.nyc_output/

# Data (runtime-generated)
data/

# R-Knowledge runtime artefacts (MIP-014) — eliminated on rollback per
# STEP 1 § 19.4. Listed explicitly as well as covered by data/, because the
# elimination policy names each file and an ignore rule that depends on a
# directory being used is weaker than one that names the artefact.
knowledge.db
knowledge.db-wal
knowledge.db-shm
knowledge-quarantine.jsonl
benchmarks/knowledge/output/

# Release artifacts
release-artifacts/

# IDE
.vscode/
.idea/

# OS
Thumbs.db
.env.docker

# Local production configuration and recovery copies
.env.production
.env.production.*
docker-compose*.bak*
dist_patches/
