Here is the exact prompt you should paste into ChatGPT (Codex) to generate the code for the hackathon submission. This ensures you get the Session ID required by Devpost.

--- PASTE EVERYTHING BELOW THIS LINE INTO CHATGPT ---

Please act as an expert Node.js and React developer. I am building a prototype called "RONOR Model Exchange v0.1" for the OpenAI Build Week hackathon. It is a Sovereign Generative Intelligence Runtime that orchestrates multiple AI models through a unified API.

I need you to generate the core backend logic and the React dashboard.

1. Backend (Node.js/Express):
- Build a Unified Request API at `POST /api/query` that accepts `{ query, task_type, confidentiality_level, max_cost, max_latency, required_evidence_level, allowed_providers }`.
- Build a Model Registry with three engines: OpenAI GPT-4.1 (real API call), Anthropic Claude Sonnet 4 (simulated), and a Deterministic Core (local math evaluator). Each must declare capabilities, cost, latency, quality, and sovereignty levels.
- Build a Policy Engine that filters eligible models based on constraints (e.g., if confidentiality="sovereign", exclude cloud models).
- Build a Dynamic Router that scores eligible models using the formula: `Score = Quality - Cost - Latency - OperationalRisk + Sovereignty + EvidenceReliability`. It must select the highest-scoring model.
- Build a Trace Ledger that records every request with a hash-chained trace ID.
- Build a Cost Ledger that tracks tokens and USD cost per model.
- Build an R-Assurance layer that scores the confidence of the output and checks for source attribution.

2. Frontend (React + Vite):
- Build a dark-themed operator dashboard (hex colors only, no oklch).
- Include a Query Console to submit requests.
- Include an Answer Panel showing the verified confidence gauge and source attribution.
- Include a Routing Panel that displays the transparent scoring table (showing exactly why a model was selected) and the Policy Engine evaluations.
- Include Cost and Trace Ledger panels.
- Include a visual diagram of the 7 operational planes (Access, Policy, Routing, Execution, Assurance, Economic, Evidence).

Please provide the complete code for the Express server (`server/index.js`, `server/router.js`, `server/engines.js`, etc.) and the React frontend (`client/src/App.jsx`, styles, etc.).

--- PASTE EVERYTHING ABOVE THIS LINE INTO CHATGPT ---
